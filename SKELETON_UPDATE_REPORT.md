# Skeleton Update Report

**Generated:** 2026-01-29 20:26:18
**Skeleton:** `mcp-server-skeleton-ideal/`

This report compares the skeleton template with official examples and extracted patterns.

---

## 1. SDK Version Check

| Package | Skeleton | Example | Status |
|---------|----------|---------|--------|
| @modelcontextprotocol/ext-apps | 1.0.0 | 1.0.0 | OK |
| @modelcontextprotocol/sdk | 1.25.1 | 1.24.0 | OK (newer) |
| zod | 4.1.13 | 4.1.13 | OK |

## 2. Server Registration Patterns

- [x] Uses nested `_meta.ui.resourceUri` (v1.0.0 stable pattern)
- [x] Uses Zod v4 import (`zod/v4`)
- [x] Uses `.meta()` for descriptions (Zod v4)
- [x] Returns `structuredContent` for UI widgets

## 3. Widget Patterns

- [x] Uses `useApp` hook
- [x] Uses `onAppCreated` callback for handlers
- [x] Has `ontoolresult` handler
- [x] Has `onteardown` handler
- [x] Uses 600px height pattern
- [x] Has v1.0.0 `ontoolinputpartial` handler
- [x] Has v1.0.0 `viewUUID` support

## 4. Build Configuration

- [x] Uses `viteSingleFile` plugin
- [x] Has `emptyOutDir: false`
- [x] Uses `cross-env` for scripts
- [x] Has `concurrently` for dev workflow

---

## Summary

**Total updates needed:** 0

Skeleton is up to date with official patterns.

---

*Generated by scripts/update-skeleton.sh*
